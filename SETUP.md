# 快速上手指南

## ✅ 环境已就绪

- Python 3.9.6 + 虚拟环境 `.venv`
- 所有依赖已安装（包括PyBullet 3.2.7）
- 代码已测试通过（100%）

## 🚀 快速开始

### 1. 激活环境
```bash
source .venv/bin/activate  # macOS/Linux
```

### 2. 验证安装
```bash
python verify_setup.py  # 应该100%通过
```

### 3. 运行仿真

**快速测试（无GUI，3秒）**:
```bash
python test_simulation.py
```

**交互式GUI（可调节关节）**:
```bash
python src/simulation/environment.py
# 用滑块调整关节，Ctrl+C退出
```

**站立稳定性测试**:
```bash
python tests/test_standing.py --mode basic --duration 5
```

## 📁 核心文件

| 文件 | 说明 |
|------|------|
| `README.md` | 项目总览 |
| `SETUP.md` | 本文档 |
| `config/robot_config.yaml` | 机器人参数配置 |
| `models/humanoid_v1.urdf` | 机器人模型（已生成） |
| `docs/getting_started.md` | 30分钟入门教程 |
| `docs/roadmap.md` | 开发路线图 |

## 🔧 常用操作

### 修改机器人参数
1. 编辑 `config/robot_config.yaml`
2. 运行 `python src/robot/urdf_generator.py`
3. 测试新模型

### 查看项目结构
```bash
src/
├── robot/          # 机器人建模
├── simulation/     # 仿真环境
├── control/        # 控制算法（第二阶段）
└── utils/          # 工具函数
```

## ⚠️ 已修复的Bug

- ✅ PyBullet退出时的参数读取错误已修复
- ✅ 连接状态检查已添加

## 📚 深入学习

1. **30分钟入门**: `docs/getting_started.md`
2. **完整指南**: `docs/phase1_guide.md`
3. **系统架构**: `docs/architecture.md`
4. **开发路线**: `docs/roadmap.md`

## 🎯 当前状态

✅ 第一阶段：仿真建模 - **完成**  
🔜 第二阶段：控制算法 - 准备开始  

---

有问题？运行 `python verify_setup.py` 检查环境。
